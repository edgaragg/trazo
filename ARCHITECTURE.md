<!-- trazo-architecture -->
# Architecture

Generated by [Trazo](https://github.com/edgaragg/trazo) from this repository's infrastructure files. Do not edit by hand — regenerate it with `trazo generate --format markdown --out ARCHITECTURE.md`.

```mermaid
flowchart LR
  n_api["api"]
  n_cache(["cache"])
  n_db[("db")]
  n_queue>"queue"]
  n_web["web"]
  n_worker["worker"]
  n_api --> n_cache
  n_api --> n_db
  n_api --> n_queue
  n_web --> n_api
  n_worker --> n_db
  n_worker --> n_queue
```

## Components

- `api` (service)
- `cache` (cache, `redis:7`)
- `db` (database, `postgres:16`)
- `queue` (queue, `rabbitmq:3-management`)
- `web` (service)
- `worker` (service)

## Dependencies

- `api` → `cache`
- `api` → `db`
- `api` → `queue`
- `web` → `api`
- `worker` → `db`
- `worker` → `queue`
